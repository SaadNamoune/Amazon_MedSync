--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.1+cu121
torchvision==0.19.1+cu121

nvflare==2.5.1
opacus==1.5.2
datasets==3.0.1
huggingface_hub==0.25.1
kaggle==2.2.4
mlflow==2.16.2
fastapi==0.115.0
uvicorn[standard]==0.30.6
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
pillow==10.4.0
tqdm==4.66.5
requests==2.32.3
python-multipart==0.0.9
sqlalchemy==2.0.16
bcrypt==5.0.0
pyjwt==2.13.0

# monai is NOT listed above on purpose: monai==1.6.0 hard-requires
# torch>=2.8.0, which conflicts with the pinned torch==2.4.1+cu121 above
# (needed for nvflare/opacus compatibility) and would break `pip install
# -r requirements.txt` entirely if both were resolved together. Its
# DenseNet121 network class works fine against torch 2.4.1 in practice
# (tested), so install it as a separate step, bypassing dependency
# resolution:
#   pip install --no-deps monai==1.6.0
